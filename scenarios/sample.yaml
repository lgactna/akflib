# Run with one of the following:
#   akf-translate scenarios/sample.yaml --translate
#   akf-translate scenarios/sample.yaml --execute --machine-name none

name: sample scenario
description: sample scenario
author: lgactna
seed: "0"
libraries:
  - akflib.actions
actions:
  - name: Run the sample action
    module: akflib.actions.sample.SampleModule
    args:
      arg1: "value1"
      arg2: "value2"